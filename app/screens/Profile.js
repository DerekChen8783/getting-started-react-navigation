import React, { Component } from 'react';
import { ScrollView, Text } from 'react-native';

class Profile extends Component {
  render() {
    return (
      <ScrollView>
        <Text>
Hi, there! Welcome to Profile page.
            </Text>
        
      </ScrollView>
    );
  }
}

export default Profile;
