import React, { Component } from 'react';
import { ScrollView, Text } from 'react-native';

class Notifications extends Component {
  render() {
    return (
      <ScrollView>
        <Text>
Hi, there! Welcome to Notifications page.
            </Text>
        
      </ScrollView>
    );
  }
}

export default Notifications;
